spring:
  application:
    name: skillbox-hotels
  data:
    mongodb:
      host: mongo
      password: secret
      username: root
      database: hotels
  datasource:
    url: jdbc:postgresql://postgres:5432/hotels
    username: myuser
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false