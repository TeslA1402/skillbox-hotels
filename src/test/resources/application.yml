spring:
  application:
    name: skillbox-hotels
  data:
    mongodb:
      host: localhost
      password: secret
      username: root
      database: hotels
  datasource:
    url: jdbc:h2:mem:public
    username: testUser
    password: testPassword
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false